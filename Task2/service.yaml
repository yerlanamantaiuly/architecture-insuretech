apiVersion: v1
kind: Service
metadata:
  name: insuretech-demo-app
  labels:
    app: insuretech-demo-app
spec:
  type: ClusterIP
  selector:
    app: insuretech-demo-app
  ports:
    - name: http
      port: 8080
      targetPort: http
